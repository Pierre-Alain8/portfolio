import React from "react";
import { withRouter } from "react-router-dom";

const Projects = (props) => {
  return <section className="projects-portfolio">hello projects</section>;
};

export default withRouter(Projects);
